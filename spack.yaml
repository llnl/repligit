# This is a Spack Environment file.
#
# It describes a set of packages to be installed, along with
# configuration settings.
spack:
  repos:
    repligit: spack/repos/spack_repo/repligit
  specs:
  - py-codespell
  - py-pytest
  - py-repligit +aiohttp
  - py-bandit
  - py-ruff
  - py-pip
  - py-ty
  - py-build
  - py-hatch-vcs
  view: true
  concretizer:
    unify: true
  develop:
    py-repligit:
      spec: py-repligit@=main
      path: .
